<div>
    {{-- Markup untuk __STUDLY__. Logika bisnis TIDAK boleh masuk sini —
         kondisi lebih dari perbandingan sederhana pindahkan ke accessor Model
         atau computed property di component. --}}

    <form wire:submit="save">
        {{-- @error('field') <span class="text-red-600">{{ $message }}</span> @enderror --}}

        <button type="submit">Simpan</button>
    </form>
</div>
